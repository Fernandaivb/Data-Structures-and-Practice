#include <stdio.h>
#include <string.h>

int main() {
    char string[100];
    scanf("%s",string);
    
    int strlength = strlen(string);
    
    for (int i=length-1; i>=0; i--)
        string = string[i];
        printf("%s", string);
    return 0;
}
